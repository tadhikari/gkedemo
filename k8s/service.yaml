apiVersion: v1
kind: Service
metadata:
  name: spring-boot-service
spec:
  type: LoadBalancer
  selector:
    app: spring-boot
  ports:
    - protocol: TCP
      port: 8080       # External port
      targetPort: 8080 # Container port